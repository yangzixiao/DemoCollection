<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yzx.democollection">


    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".DemoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DemoCollection">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <activity
            android:name=".ui.bottomnavigationview.BottomNavigationViewActivity"
            android:label="BottomNavigationViewActivity" />
        <activity
            android:name=".ui.search.SearchActivity"
            android:theme="@style/SearchActivity" />
        <activity
            android:name=".ui.livedata.LiveDataActivity"
            android:label="LiveDataActivity" />
        <activity android:name=".ui.badgedrawable.BadgeDrawableActivity" />
        <activity android:name=".ui.trend.TrendActivity" />
        <activity android:name=".ui.trend.trend2.lottery.LottoTrendActivity" />
        <activity android:name=".ui.chip.ChipActivity" />
    </application>

</manifest>